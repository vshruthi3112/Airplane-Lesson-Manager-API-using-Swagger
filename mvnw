#!/bin/sh
# Maven Wrapper script - downloads Maven if not installed
# For full wrapper, generate it with: mvn wrapper:wrapper
echo "To use the Maven wrapper, generate it with:"
echo "  mvn wrapper:wrapper"
echo ""
echo "Or run directly with:"
echo "  mvn spring-boot:run"
